Place real client logo files here, e.g.:
  adani.png
  tata.png
  amns.png
  ntpc.png
  nalco.png
  vedanta.png

Then in components/OurClients.tsx, change the matching client's
`logo: null` to `logo: '/clients/adani.png'` (etc.)

Tip: transparent PNG or SVG, roughly 200x80px, looks best on the white card background.
